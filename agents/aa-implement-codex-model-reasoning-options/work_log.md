# Work log

- Inspected the run CLI, backend construction, Codex app-server payloads, tests, and specifications.
- Added typed Codex model and reasoning-effort run overrides with environment fallback and pre-mutation validation.
- Added CLI, configuration, protocol payload, backend isolation, and state-safety tests.
- Documented option scope, precedence, accepted efforts, and protocol fields.
- Passed clippy, format, debug and release builds, 305 tests, `cargo run -- check`, and `waap check`.
